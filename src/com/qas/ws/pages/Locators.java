package com.qas.ws.pages;

public interface Locators {
	public interface OrderPageLocators {
		String ORDER_LOC = "order.loc";

		String CREATE_BTN_LOC = "createnew.button.loc";

		String ORDER_BY_NAME_OR_ID_LOC_FORMAT = "order.byname.loc.format";
		String ORDER_ID_LOC = "order.id.loc";

		String CLIENT_NAME_LOC = "order.client.loc";

		String ORDER_AMOUNT_LOC = "order.amount.loc";

		String VIEW_BTN_LOC = "view.button.loc";

		String EDIT_BTN_LOC = "edit.button.loc";

		String DELETE_BTN_LOC = "delete.button.loc";

	}
}
